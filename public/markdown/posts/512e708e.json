{
  "title": "opencode的7个基础工具——read、write、edit、multiedit、glob、grep、bash",
  "date": "2026-03-25T12:53:00.000Z",
  "updatedAt": "2026-03-25T12:53:00.000Z",
  "file": "opencode-seven-core-tools.md",
  "slug": "512e708e",
  "categories": [
    "技术教程"
  ],
  "tags": [
    "技术教程"
  ],
  "img": "",
  "wordCount": 1908,
  "readingTime": 5,
  "body": "# opencode的7个基础工具——read、write、edit、multiedit、glob、grep、bash\n\n做 AI 编程助手（或类似工具）的核心，就是怎么让 AI 正确地读写文件、执行命令。本质上，LLM 是个\"脑子里有很多知识但手很短\"的存在——它能推理，但没法直接摸到你的代码。于是需要一套工具，让它能够安全、可控地操作文件系统。\n\nOpenCode（以及其他同类工具如 Claude Code、Cursor）把这套工具抽象成了 7 个核心 API。下面逐一拆解。\n\n---\n\n## 1. read — 统一的文件/目录读取入口\n\n### 它是干什么的\n\n一个字：**只读**。AI 想看代码，先走这个入口。\n\n### 设计思想\n\n一个工具同时处理文件和目录，通过检测文件类型来自动决定怎么展示。\n\n**文件读取**方面：\n- 支持分页，`offset` 和 `limit` 参数让 AI 可以\"翻页\"读大文件\n- 自动给每行加上行号，方便 AI 定位（比如报错说\"第 42 行有问题\"）\n- 超长的行会自动截断，避免把整个终端撑爆\n\n**目录读取**方面：\n- 返回目录结构，树状展开\n- 自动识别符号链接（快捷方式），不会当成普通文件处理\n\n**特殊格式**（图片、PDF）：\n- 直接作为附件返回，用 Base64 编码，AI 看到的就是图片本身而不是乱码\n- 二进制文件（非文本）通过采样检测，不尝试强行解码\n\n**一个有意思的细节**：读取文件后会\"假装碰了一下\"文件（`touch`），这样会触发 LSP（Language Server Protocol）预加载——相当于提前告诉IDE\"这个文件你该关注了\"，下次分析代码能更快。\n\n### 核心原则\n\n> 只读操作，不修改任何内容\n\n---\n\n## 2. write — 覆盖式写入 + 自动 diff 生成\n\n### 它是干什么的\n\n写入文件内容，**全量覆盖**。\n\n### 设计思想\n\n这个工具的设计非常谨慎，强调\"先确认再执行，写入后验证\"。\n\n**幂等性 + diff 确认**：写入前，工具会对比旧内容和新内容，生成一个 unified diff（就是你在 git diff 里看到的那种格式），展示给用户看\"哪里变了\"。这样用户能明确知道 AI 想干什么，然后才授权执行。\n\n**写入后诊断**：写完之后，立刻触发 LSP 诊断，返回当前文件的错误和警告。如果写入把代码写坏了，能马上知道。\n\n**事件通知**：写入完成后发布一个 `File.Edited` 事件，其他模块（比如缓存管理器、依赖分析器）收到通知后可以同步更新状态。\n\n### 核心原则\n\n> 先确认再执行，写入后验证\n\n---\n\n## 3. edit — 智能替换 + 多策略兜底\n\n### 它是干什么的\n\n基于字符串匹配做**局部替换**——这是最常用的编辑工具。\n\n### 设计思想\n\nedit 的核心是 `replace(oldString, newString)`：找到旧的字符串，替换成新的。\n\n但问题是：AI 给出的 oldString 可能和真实代码有细微差别（比如多了几个空格、缩进不一样）。如果直接匹配不上就失败，用户体验会非常差。\n\n所以这里设计了一整套 **Replacer 策略，按顺序尝试**：\n\n1. **SimpleReplacer**：精确匹配，一模一样才替换\n2. **LineTrimmedReplacer**：忽略行首尾空格后匹配\n3. **BlockAnchorReplacer**：用首尾行做锚点，中间部分相似度匹配\n4. **WhitespaceNormalizedReplacer**：把多个空格/ tab 归一化成一种再匹配\n5. **IndentationFlexibleReplacer**：缩进灵活匹配（允许不同深度的缩进）\n6. **EscapeNormalizedReplacer**：处理转义字符（比如 `\\n` 和真正换行的差异）\n7. **TrimmedBoundaryReplacer**：裁剪边界后匹配\n8. **ContextAwareReplacer**：参考上下文判断是否匹配\n\n简单说：**如果精确匹配失败了，不要立刻放弃，换个策略再试试**。这样能大幅提高 edit 的成功率。\n\n另外还会自动检测原文件的换行符风格（`\\n` 还是 `\\r\\n`），保持原样不动。\n\n### 核心原则\n\n> 精确替换，智能兜底，保留原格式\n\n---\n\n## 4. multiedit — 批量顺序编辑\n\n### 它是干什么的\n\n一次确认，执行**多次** edit。\n\n### 设计思想\n\n内部其实就是循环调用 edit，但有一个关键约束：**按顺序执行**。每次 edit 都基于前一次 edit 之后的结果来继续。这样保证了多次修改之间不会互相干扰。\n\n返回的结果只展示最后一次 edit 的输出和元数据。\n\n### 核心原则\n\n> 化零为整，一次确认多次修改\n\n---\n\n## 5. glob — 基于 Ripgrep 的模式匹配\n\n### 它是干什么的\n\n用 glob 模式（如 `**/*.ts`）搜索文件路径。\n\n### 设计思想\n\n很多新手实现 glob 可能直接用 `fs.glob`，但 opencode 选择用 **Ripgrep** 来做底层遍历。Ripgrep 是 Rust 写的高性能搜索工具，比 Node.js 的 glob 模块快得多。\n\n额外做了几件事：\n\n- **时间排序**：搜索结果按修改时间**倒序**，最新的文件排在前面。AI 通常更需要关注新代码。\n- **数量限制**：默认最多返回 100 个结果，防止一次搜出几千个文件把上下文撑爆。\n- **权限控制**：搜索前请求 glob 权限，明确告知 AI 会访问哪些路径。\n\n### 核心原则\n\n> 高性能、结果可预期、限制输出\n\n---\n\n## 6. grep — 内容搜索 + 结果结构化\n\n### 它是干什么的\n\n在文件内容中搜索正则表达式。\n\n### 设计思想\n\n底层也是调用 Ripgrep（`rg` 命令），解析输出为结构化格式：`filePath|lineNum|content`。\n\n同样按文件修改时间倒序排列。有意思的是容错处理：如果遇到 broken symlink（损坏的快捷方式），会忽略它继续搜；只有在前两层（文件读取和内容解析）都出问题时才真正失败。\n\n### 核心原则\n\n> 精准搜索，结构化输出\n\n---\n\n## 7. bash — 命令执行 + 静态分析 + 权限请求\n\n### 它是干什么的\n\n执行 shell 命令，比如 `npm install`、`git commit`、`python run.py`。\n\n### 设计思想\n\n这是最危险的工具——也是最需要谨慎设计的。\n\n**静态分析在先**：执行命令前，先用 tree-sitter（一个代码解析库）解析命令文本，提取出会涉及哪些路径。比如 `cd src && rm -rf *.log`，能识别出涉及 `src` 目录。\n\n**权限预请求**：把命令转换成权限模式（如 `npm install` → `npm *`），在执行前向用户请求 `bash` 权限和 `external_directory` 权限。明确告诉用户：我要执行这个命令，它会访问这些路径，你同意吗？\n\n**流式输出**：命令的 stdout 和 stderr 实时流式返回，AI 能看到执行过程，有问题随时调整策略。\n\n> ⚠️ 注意：bash 无法处理交互式输入（比如要求输入密码的 `sudo` 命令、vim 编辑器等），这类场景需要特殊处理。\n\n### 核心原则\n\n> 分析先行，权限明确，安全执行\n\n---\n\n## 总结对比\n\n| 工具 | 操作方式 | 核心特点 |\n|------|----------|----------|\n| **read** | 只读 | 统一入口，自动处理多种格式 |\n| **write** | 覆盖 | 全量替换，diff 确认 |\n| **edit** | 替换 | 多策略兜底，保留格式 |\n| **multiedit** | 批量替换 | 复用 edit，顺序执行 |\n| **glob** | 搜索 | 模式匹配，时间排序 |\n| **grep** | 搜索 | 正则匹配，结构化结果 |\n| **bash** | 执行 | 命令分析，权限预请求 |\n\n---\n\n这套工具设计的精髓在于：**给 AI 足够的操作能力，同时用幂等性、diff 确认、权限预请求等机制把风险降到最低**。没有哪家公司的 AI 编程工具是靠单一绝招成功的——都是在这些细节上慢慢打磨，让 AI 的手变得越来越稳定。\n",
  "html": "<h1 id=\"opencode的7个基础工具readwriteeditmultieditglobgrepbash\">opencode的7个基础工具——read、write、edit、multiedit、glob、grep、bash</h1>\n<p>做 AI 编程助手（或类似工具）的核心，就是怎么让 AI 正确地读写文件、执行命令。本质上，LLM 是个&quot;脑子里有很多知识但手很短&quot;的存在——它能推理，但没法直接摸到你的代码。于是需要一套工具，让它能够安全、可控地操作文件系统。</p>\n<p>OpenCode（以及其他同类工具如 Claude Code、Cursor）把这套工具抽象成了 7 个核心 API。下面逐一拆解。</p>\n<hr>\n<h2 id=\"1-read-统一的文件目录读取入口\">1. read — 统一的文件/目录读取入口</h2>\n<h3 id=\"它是干什么的\">它是干什么的</h3>\n<p>一个字：<strong>只读</strong>。AI 想看代码，先走这个入口。</p>\n<h3 id=\"设计思想\">设计思想</h3>\n<p>一个工具同时处理文件和目录，通过检测文件类型来自动决定怎么展示。</p>\n<p><strong>文件读取</strong>方面：</p>\n<ul>\n<li>支持分页，<code>offset</code> 和 <code>limit</code> 参数让 AI 可以&quot;翻页&quot;读大文件</li>\n<li>自动给每行加上行号，方便 AI 定位（比如报错说&quot;第 42 行有问题&quot;）</li>\n<li>超长的行会自动截断，避免把整个终端撑爆</li>\n</ul>\n<p><strong>目录读取</strong>方面：</p>\n<ul>\n<li>返回目录结构，树状展开</li>\n<li>自动识别符号链接（快捷方式），不会当成普通文件处理</li>\n</ul>\n<p><strong>特殊格式</strong>（图片、PDF）：</p>\n<ul>\n<li>直接作为附件返回，用 Base64 编码，AI 看到的就是图片本身而不是乱码</li>\n<li>二进制文件（非文本）通过采样检测，不尝试强行解码</li>\n</ul>\n<p><strong>一个有意思的细节</strong>：读取文件后会&quot;假装碰了一下&quot;文件（<code>touch</code>），这样会触发 LSP（Language Server Protocol）预加载——相当于提前告诉IDE&quot;这个文件你该关注了&quot;，下次分析代码能更快。</p>\n<h3 id=\"核心原则\">核心原则</h3>\n<blockquote>\n<p>只读操作，不修改任何内容</p>\n</blockquote>\n<hr>\n<h2 id=\"2-write-覆盖式写入-自动-diff-生成\">2. write — 覆盖式写入 + 自动 diff 生成</h2>\n<h3 id=\"它是干什么的\">它是干什么的</h3>\n<p>写入文件内容，<strong>全量覆盖</strong>。</p>\n<h3 id=\"设计思想\">设计思想</h3>\n<p>这个工具的设计非常谨慎，强调&quot;先确认再执行，写入后验证&quot;。</p>\n<p><strong>幂等性 + diff 确认</strong>：写入前，工具会对比旧内容和新内容，生成一个 unified diff（就是你在 git diff 里看到的那种格式），展示给用户看&quot;哪里变了&quot;。这样用户能明确知道 AI 想干什么，然后才授权执行。</p>\n<p><strong>写入后诊断</strong>：写完之后，立刻触发 LSP 诊断，返回当前文件的错误和警告。如果写入把代码写坏了，能马上知道。</p>\n<p><strong>事件通知</strong>：写入完成后发布一个 <code>File.Edited</code> 事件，其他模块（比如缓存管理器、依赖分析器）收到通知后可以同步更新状态。</p>\n<h3 id=\"核心原则\">核心原则</h3>\n<blockquote>\n<p>先确认再执行，写入后验证</p>\n</blockquote>\n<hr>\n<h2 id=\"3-edit-智能替换-多策略兜底\">3. edit — 智能替换 + 多策略兜底</h2>\n<h3 id=\"它是干什么的\">它是干什么的</h3>\n<p>基于字符串匹配做<strong>局部替换</strong>——这是最常用的编辑工具。</p>\n<h3 id=\"设计思想\">设计思想</h3>\n<p>edit 的核心是 <code>replace(oldString, newString)</code>：找到旧的字符串，替换成新的。</p>\n<p>但问题是：AI 给出的 oldString 可能和真实代码有细微差别（比如多了几个空格、缩进不一样）。如果直接匹配不上就失败，用户体验会非常差。</p>\n<p>所以这里设计了一整套 <strong>Replacer 策略，按顺序尝试</strong>：</p>\n<ol>\n<li><strong>SimpleReplacer</strong>：精确匹配，一模一样才替换</li>\n<li><strong>LineTrimmedReplacer</strong>：忽略行首尾空格后匹配</li>\n<li><strong>BlockAnchorReplacer</strong>：用首尾行做锚点，中间部分相似度匹配</li>\n<li><strong>WhitespaceNormalizedReplacer</strong>：把多个空格/ tab 归一化成一种再匹配</li>\n<li><strong>IndentationFlexibleReplacer</strong>：缩进灵活匹配（允许不同深度的缩进）</li>\n<li><strong>EscapeNormalizedReplacer</strong>：处理转义字符（比如 <code>\\n</code> 和真正换行的差异）</li>\n<li><strong>TrimmedBoundaryReplacer</strong>：裁剪边界后匹配</li>\n<li><strong>ContextAwareReplacer</strong>：参考上下文判断是否匹配</li>\n</ol>\n<p>简单说：<strong>如果精确匹配失败了，不要立刻放弃，换个策略再试试</strong>。这样能大幅提高 edit 的成功率。</p>\n<p>另外还会自动检测原文件的换行符风格（<code>\\n</code> 还是 <code>\\r\\n</code>），保持原样不动。</p>\n<h3 id=\"核心原则\">核心原则</h3>\n<blockquote>\n<p>精确替换，智能兜底，保留原格式</p>\n</blockquote>\n<hr>\n<h2 id=\"4-multiedit-批量顺序编辑\">4. multiedit — 批量顺序编辑</h2>\n<h3 id=\"它是干什么的\">它是干什么的</h3>\n<p>一次确认，执行<strong>多次</strong> edit。</p>\n<h3 id=\"设计思想\">设计思想</h3>\n<p>内部其实就是循环调用 edit，但有一个关键约束：<strong>按顺序执行</strong>。每次 edit 都基于前一次 edit 之后的结果来继续。这样保证了多次修改之间不会互相干扰。</p>\n<p>返回的结果只展示最后一次 edit 的输出和元数据。</p>\n<h3 id=\"核心原则\">核心原则</h3>\n<blockquote>\n<p>化零为整，一次确认多次修改</p>\n</blockquote>\n<hr>\n<h2 id=\"5-glob-基于-ripgrep-的模式匹配\">5. glob — 基于 Ripgrep 的模式匹配</h2>\n<h3 id=\"它是干什么的\">它是干什么的</h3>\n<p>用 glob 模式（如 <code>**/*.ts</code>）搜索文件路径。</p>\n<h3 id=\"设计思想\">设计思想</h3>\n<p>很多新手实现 glob 可能直接用 <code>fs.glob</code>，但 opencode 选择用 <strong>Ripgrep</strong> 来做底层遍历。Ripgrep 是 Rust 写的高性能搜索工具，比 Node.js 的 glob 模块快得多。</p>\n<p>额外做了几件事：</p>\n<ul>\n<li><strong>时间排序</strong>：搜索结果按修改时间<strong>倒序</strong>，最新的文件排在前面。AI 通常更需要关注新代码。</li>\n<li><strong>数量限制</strong>：默认最多返回 100 个结果，防止一次搜出几千个文件把上下文撑爆。</li>\n<li><strong>权限控制</strong>：搜索前请求 glob 权限，明确告知 AI 会访问哪些路径。</li>\n</ul>\n<h3 id=\"核心原则\">核心原则</h3>\n<blockquote>\n<p>高性能、结果可预期、限制输出</p>\n</blockquote>\n<hr>\n<h2 id=\"6-grep-内容搜索-结果结构化\">6. grep — 内容搜索 + 结果结构化</h2>\n<h3 id=\"它是干什么的\">它是干什么的</h3>\n<p>在文件内容中搜索正则表达式。</p>\n<h3 id=\"设计思想\">设计思想</h3>\n<p>底层也是调用 Ripgrep（<code>rg</code> 命令），解析输出为结构化格式：<code>filePath|lineNum|content</code>。</p>\n<p>同样按文件修改时间倒序排列。有意思的是容错处理：如果遇到 broken symlink（损坏的快捷方式），会忽略它继续搜；只有在前两层（文件读取和内容解析）都出问题时才真正失败。</p>\n<h3 id=\"核心原则\">核心原则</h3>\n<blockquote>\n<p>精准搜索，结构化输出</p>\n</blockquote>\n<hr>\n<h2 id=\"7-bash-命令执行-静态分析-权限请求\">7. bash — 命令执行 + 静态分析 + 权限请求</h2>\n<h3 id=\"它是干什么的\">它是干什么的</h3>\n<p>执行 shell 命令，比如 <code>npm install</code>、<code>git commit</code>、<code>python run.py</code>。</p>\n<h3 id=\"设计思想\">设计思想</h3>\n<p>这是最危险的工具——也是最需要谨慎设计的。</p>\n<p><strong>静态分析在先</strong>：执行命令前，先用 tree-sitter（一个代码解析库）解析命令文本，提取出会涉及哪些路径。比如 <code>cd src &amp;&amp; rm -rf *.log</code>，能识别出涉及 <code>src</code> 目录。</p>\n<p><strong>权限预请求</strong>：把命令转换成权限模式（如 <code>npm install</code> → <code>npm *</code>），在执行前向用户请求 <code>bash</code> 权限和 <code>external_directory</code> 权限。明确告诉用户：我要执行这个命令，它会访问这些路径，你同意吗？</p>\n<p><strong>流式输出</strong>：命令的 stdout 和 stderr 实时流式返回，AI 能看到执行过程，有问题随时调整策略。</p>\n<blockquote>\n<p>⚠️ 注意：bash 无法处理交互式输入（比如要求输入密码的 <code>sudo</code> 命令、vim 编辑器等），这类场景需要特殊处理。</p>\n</blockquote>\n<h3 id=\"核心原则\">核心原则</h3>\n<blockquote>\n<p>分析先行，权限明确，安全执行</p>\n</blockquote>\n<hr>\n<h2 id=\"总结对比\">总结对比</h2>\n<table>\n<thead>\n<tr>\n<th>工具</th>\n<th>操作方式</th>\n<th>核心特点</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>read</strong></td>\n<td>只读</td>\n<td>统一入口，自动处理多种格式</td>\n</tr>\n<tr>\n<td><strong>write</strong></td>\n<td>覆盖</td>\n<td>全量替换，diff 确认</td>\n</tr>\n<tr>\n<td><strong>edit</strong></td>\n<td>替换</td>\n<td>多策略兜底，保留格式</td>\n</tr>\n<tr>\n<td><strong>multiedit</strong></td>\n<td>批量替换</td>\n<td>复用 edit，顺序执行</td>\n</tr>\n<tr>\n<td><strong>glob</strong></td>\n<td>搜索</td>\n<td>模式匹配，时间排序</td>\n</tr>\n<tr>\n<td><strong>grep</strong></td>\n<td>搜索</td>\n<td>正则匹配，结构化结果</td>\n</tr>\n<tr>\n<td><strong>bash</strong></td>\n<td>执行</td>\n<td>命令分析，权限预请求</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<p>这套工具设计的精髓在于：<strong>给 AI 足够的操作能力，同时用幂等性、diff 确认、权限预请求等机制把风险降到最低</strong>。没有哪家公司的 AI 编程工具是靠单一绝招成功的——都是在这些细节上慢慢打磨，让 AI 的手变得越来越稳定。</p>\n",
  "headings": [
    {
      "level": 1,
      "text": "opencode的7个基础工具——read、write、edit、multiedit、glob、grep、bash",
      "id": "opencode的7个基础工具readwriteeditmultieditglobgrepbash"
    },
    {
      "level": 2,
      "text": "1. read — 统一的文件/目录读取入口",
      "id": "1-read-统一的文件目录读取入口"
    },
    {
      "level": 3,
      "text": "它是干什么的",
      "id": "它是干什么的"
    },
    {
      "level": 3,
      "text": "设计思想",
      "id": "设计思想"
    },
    {
      "level": 3,
      "text": "核心原则",
      "id": "核心原则"
    },
    {
      "level": 2,
      "text": "2. write — 覆盖式写入 + 自动 diff 生成",
      "id": "2-write-覆盖式写入-自动-diff-生成"
    },
    {
      "level": 3,
      "text": "它是干什么的",
      "id": "它是干什么的"
    },
    {
      "level": 3,
      "text": "设计思想",
      "id": "设计思想"
    },
    {
      "level": 3,
      "text": "核心原则",
      "id": "核心原则"
    },
    {
      "level": 2,
      "text": "3. edit — 智能替换 + 多策略兜底",
      "id": "3-edit-智能替换-多策略兜底"
    },
    {
      "level": 3,
      "text": "它是干什么的",
      "id": "它是干什么的"
    },
    {
      "level": 3,
      "text": "设计思想",
      "id": "设计思想"
    },
    {
      "level": 3,
      "text": "核心原则",
      "id": "核心原则"
    },
    {
      "level": 2,
      "text": "4. multiedit — 批量顺序编辑",
      "id": "4-multiedit-批量顺序编辑"
    },
    {
      "level": 3,
      "text": "它是干什么的",
      "id": "它是干什么的"
    },
    {
      "level": 3,
      "text": "设计思想",
      "id": "设计思想"
    },
    {
      "level": 3,
      "text": "核心原则",
      "id": "核心原则"
    },
    {
      "level": 2,
      "text": "5. glob — 基于 Ripgrep 的模式匹配",
      "id": "5-glob-基于-ripgrep-的模式匹配"
    },
    {
      "level": 3,
      "text": "它是干什么的",
      "id": "它是干什么的"
    },
    {
      "level": 3,
      "text": "设计思想",
      "id": "设计思想"
    },
    {
      "level": 3,
      "text": "核心原则",
      "id": "核心原则"
    },
    {
      "level": 2,
      "text": "6. grep — 内容搜索 + 结果结构化",
      "id": "6-grep-内容搜索-结果结构化"
    },
    {
      "level": 3,
      "text": "它是干什么的",
      "id": "它是干什么的"
    },
    {
      "level": 3,
      "text": "设计思想",
      "id": "设计思想"
    },
    {
      "level": 3,
      "text": "核心原则",
      "id": "核心原则"
    },
    {
      "level": 2,
      "text": "7. bash — 命令执行 + 静态分析 + 权限请求",
      "id": "7-bash-命令执行-静态分析-权限请求"
    },
    {
      "level": 3,
      "text": "它是干什么的",
      "id": "它是干什么的"
    },
    {
      "level": 3,
      "text": "设计思想",
      "id": "设计思想"
    },
    {
      "level": 3,
      "text": "核心原则",
      "id": "核心原则"
    },
    {
      "level": 2,
      "text": "总结对比",
      "id": "总结对比"
    }
  ],
  "hasMath": false
}